# 权限功能测试报告

- **测试日期**:2026-06-03
- **测试方式**:接口自动化(直接调 HTTP API),基于干净种子数据
- **结果**:**25 / 25 全部通过 ✅**
- **测试后**:数据库已复位为干净种子,可照本报告逐条复验

> 复验前置:`go run ./tools/dbinit` → `go run main.go` → 浏览器 http://127.0.0.1:8000/
> 种子用户:张三(uid=1,安防管理员)、李四(uid=2,园区A值班员)、王五(uid=3,双角色)

---

## 种子数据(测试基线)

- **区域树**:根区域(1) >(事件图片测试(2) / 园区A(3) >(1号楼(4)、2号楼(5)))
- **资源**:101枪机@事件图片测试、102球机/103电梯口@1号楼、104停车场@2号楼
- **角色**:
  - 安防管理员:全部菜单;管理区域=事件图片测试子树;资源范围=事件图片测试子树
  - 园区A值班员:部分应用菜单;资源范围=园区A子树;**102球机精细=仅实时预览**

---

## 一、功能权限(菜单)

| 编号 | 场景 | 预期 | 实际 | 状态 |
|---|---|---|---|---|
| A1 | 李四访问应用菜单「实时预览」(有) | 允许 | 允许 | ✅ |
| A2 | 李四访问系统菜单「角色管理」(无) | 拒绝 | 拒绝 | ✅ |
| A3 | 张三访问「高级系统管理」(全菜单) | 允许 | 允许 | ✅ |

**复验**:应用端切到对应用户看顶部"可用应用菜单";或角色管理右侧「鉴权测试」选 类型=菜单。

## 二、数据权限·区域(管理域)

| 编号 | 场景 | 预期 | 实际 | 状态 |
|---|---|---|---|---|
| B1 | 张三访问直接授权的「事件图片测试」 | 允许 | 允许 | ✅ |
| B2 | 张三访问其**父**「根区域」(未授权父) | 拒绝 | 拒绝 | ✅ |
| B3 | 张三访问范围外「园区A」 | 拒绝 | 拒绝 | ✅ |
| B4 | 李四访问「园区A」管理(他只有资源域权限) | 拒绝 | 拒绝 | ✅ |

**说明**:B2 证明"只授权子节点、父节点无权"成立;路径前缀只向下匹配,不会向上覆盖父节点。
**复验**:鉴权测试 选 类型=安保区域。

## 三、业务资源权限(应用域:资源 + 操作)

| 编号 | 场景 | 预期 | 实际 | 状态 |
|---|---|---|---|---|
| C1 | 李四 102球机 实时预览(精细授予) | 允许 | 允许 | ✅ |
| C2 | 李四 102球机 远程回放(精细排除) | 拒绝 | 拒绝 | ✅ |
| C3 | 李四 104停车场 远程回放(无精细→继承全部) | 允许 | 允许 | ✅ |
| C4 | 李四 101枪机(在范围外的事件图片测试区) | 拒绝 | 拒绝 | ✅ |

**说明**:C1/C2 证明"精细覆盖模式"(配了精细就只给所列操作);C3 证明"继承模式"(范围内无精细配置默认全部操作)。
**复验**:应用端登录李四 → 点园区A → 各摄像头按钮的可点/置灰;或鉴权测试 类型=业务资源+操作。

## 四、应用端体验(可见性 vs 访问权)

| 编号 | 场景 | 预期 | 实际 | 状态 |
|---|---|---|---|---|
| E1 | 李四可见树:根区域为"仅导航祖先" | accessible=false | false | ✅ |
| E2 | 李四可见树:园区A可访问 | accessible=true | true | ✅ |
| E3 | 李四点「根区域」 | 暂无操作权限(accessible:false) | false | ✅ |
| E4 | 李四点「园区A」 | 可访问、列出子树资源 | true,含102/103/104 | ✅ |
| E5 | 李四点园区A:102球机仅实时预览有权 | live=可点,回放/图片=置灰 | 一致 | ✅ |
| E6 | 李四可用应用菜单数 | 7 | 7 | ✅ |

**说明**:E1/E3 证明"父节点能看到(导航)但点击提示暂无操作权限"。
**复验**:应用端登录李四,左侧树点根区域 vs 点园区A 对比。

## 五、二次授权(受控委派 · 模型A合并)

| 编号 | 场景 | 预期 | 实际 | 状态 |
|---|---|---|---|---|
| D1 | 李四建角色,提交[角色管理(越权), 实时预览(范围内)] | 越权项丢弃,只留实时预览 | menuIds=[107] | ✅ |
| D2 | 李四编辑"安防管理员"(全菜单)只提交[实时预览] | 范围外原有权限保留 | preserved=19 | ✅ |
| D2′| D2后,张三(绑该角色)访问范围外"高级系统管理" | 仍允许(不级联) | 允许 | ✅ |

**说明**:D1=权限不放大(越权提交被丢);D2/D2′=编辑者看不到的范围外权限被原样保留,且不影响已绑该角色的用户。
**复验**:角色管理顶部「操作者」选李四 → 树上超范围节点被隐藏 → 保存只影响其可见范围。

## 六、用户管理 + 切换登录

| 编号 | 场景 | 预期 | 实际 | 状态 |
|---|---|---|---|---|
| F1 | 新建用户绑定"园区A值班员"角色 | 创建成功 | uid 分配成功 | ✅ |
| F1′| 该用户应用菜单数 | 7(与园区A值班员一致) | 7 | ✅ |
| F1″| 该用户点根区域 | 暂无操作权限 | accessible:false | ✅ |

**复验**:用户管理 → 新建用户绑角色 → 右上角"当前登录"切到该用户 → 应用端体验。

## 七、持久化(MySQL)

| 编号 | 场景 | 预期 | 实际 | 状态 |
|---|---|---|---|---|
| G1 | 新建角色后重启服务,角色仍在 | 在,menuIds 不变 | 在,[107] | ✅ |
| G2 | 新建用户后重启服务,用户仍在 | 在 | 在 | ✅ |

**复验**:建角色/用户 → 关闭并重新 `go run main.go` → 数据仍在(从 DB 重载)。

---

## 附:未自动化、建议手工验证的点

1. **新增区域自动继承**:在区域树下(单根)新增子区域并写对 `path`,授权了上层子树的角色应自动覆盖新区域(已单独验证,见对话记录)。
2. **前端交互细节**:树展开/折叠、保存后提示、置灰按钮的点击 toast 等,建议在浏览器实际点一遍。
3. **多角色叠加**:王五(uid=3,双角色)的权限应是两角色的并集,可在应用端登录王五观察。
