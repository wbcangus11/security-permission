# 权限功能测试报告

- **测试日期**:2026-06-03(§一~§八)、2026-06-05(§九/§十)、2026-06-08(§十一/§十二)
- **测试方式**:接口自动化(直接调 HTTP API),基于干净种子数据
- **结果**:核心场景 **31/31** + 区域增删改 **11/11**(§九)+ 超管 **10/10**(§十)+ 组织增删改 **12/12**(§十一)+ 角色删除 **14/14**(§十二)+ 资源增删改 **13/13**(§十三)+ 显式角色范围/模型B **11/11**(§十四)= **全部通过 ✅**
- **测试后**:数据库已复位为干净种子(DROP + dbinit,含 AUTO_INCREMENT),可照本报告逐条复验

> 复验前置:`go run ./tools/dbinit` → `go run main.go` → 浏览器 http://127.0.0.1:8000/
> 种子用户:张三(uid=1,安防管理员)、李四(uid=2,园区A值班员)、王五(uid=3,双角色)

---

## 种子数据(测试基线)

- **区域树**:根区域(1) >(事件图片测试(2) / 园区A(3) >(1号楼(4)、2号楼(5)))
- **资源**:101枪机@事件图片测试、102球机/103电梯口@1号楼、104停车场@2号楼
- **角色**:
  - 安防管理员:全部菜单;管理区域=事件图片测试子树;资源范围=事件图片测试子树
  - 园区A值班员:部分应用菜单;资源范围=园区A子树;**102球机精细=仅实时预览**

---

## 一、功能权限(菜单)

| 编号 | 场景 | 预期 | 实际 | 状态 |
|---|---|---|---|---|
| A1 | 李四访问应用菜单「实时预览」(有) | 允许 | 允许 | ✅ |
| A2 | 李四访问系统菜单「角色管理」(无) | 拒绝 | 拒绝 | ✅ |
| A3 | 张三访问「高级系统管理」(全菜单) | 允许 | 允许 | ✅ |

**复验**:应用端切到对应用户看顶部"可用应用菜单";或角色管理右侧「鉴权测试」选 类型=菜单。

## 二、数据权限·区域(管理域)

| 编号 | 场景 | 预期 | 实际 | 状态 |
|---|---|---|---|---|
| B1 | 张三访问直接授权的「事件图片测试」 | 允许 | 允许 | ✅ |
| B2 | 张三访问其**父**「根区域」(未授权父) | 拒绝 | 拒绝 | ✅ |
| B3 | 张三访问范围外「园区A」 | 拒绝 | 拒绝 | ✅ |
| B4 | 李四访问「园区A」管理(他只有资源域权限) | 拒绝 | 拒绝 | ✅ |

**说明**:B2 证明"只授权子节点、父节点无权"成立;路径前缀只向下匹配,不会向上覆盖父节点。
**复验**:鉴权测试 选 类型=安保区域。

## 三、业务资源权限(应用域:资源 + 操作)

| 编号 | 场景 | 预期 | 实际 | 状态 |
|---|---|---|---|---|
| C1 | 李四 102球机 实时预览(精细授予) | 允许 | 允许 | ✅ |
| C2 | 李四 102球机 远程回放(精细排除) | 拒绝 | 拒绝 | ✅ |
| C3 | 李四 104停车场 远程回放(无精细→继承全部) | 允许 | 允许 | ✅ |
| C4 | 李四 101枪机(在范围外的事件图片测试区) | 拒绝 | 拒绝 | ✅ |

**说明**:C1/C2 证明"精细覆盖模式"(配了精细就只给所列操作);C3 证明"继承模式"(范围内无精细配置默认全部操作)。
**复验**:应用端登录李四 → 点园区A → 各摄像头按钮的可点/置灰;或鉴权测试 类型=业务资源+操作。

## 四、应用端体验(可见性 vs 访问权)

| 编号 | 场景 | 预期 | 实际 | 状态 |
|---|---|---|---|---|
| E1 | 李四可见树:根区域为"仅导航祖先" | accessible=false | false | ✅ |
| E2 | 李四可见树:园区A可访问 | accessible=true | true | ✅ |
| E3 | 李四点「根区域」 | 暂无操作权限(accessible:false) | false | ✅ |
| E4 | 李四点「园区A」 | 可访问、列出子树资源 | true,含102/103/104 | ✅ |
| E5 | 李四点园区A:102球机仅实时预览有权 | live=可点,回放/图片=置灰 | 一致 | ✅ |
| E6 | 李四可用应用菜单数 | 7 | 7 | ✅ |

**说明**:E1/E3 证明"父节点能看到(导航)但点击提示暂无操作权限"。
**复验**:应用端登录李四,左侧树点根区域 vs 点园区A 对比。

## 五、二次授权(受控委派 · 模型A合并)

| 编号 | 场景 | 预期 | 实际 | 状态 |
|---|---|---|---|---|
| D1 | 李四建角色,提交[角色管理(越权), 实时预览(范围内)] | 越权项丢弃,只留实时预览 | menuIds=[107] | ✅ |
| D2 | 李四编辑"安防管理员"(全菜单)只提交[实时预览] | 范围外原有权限保留 | preserved=19 | ✅ |
| D2′| D2后,张三(绑该角色)访问范围外"高级系统管理" | 仍允许(不级联) | 允许 | ✅ |

**说明**:D1=权限不放大(越权提交被丢);D2/D2′=编辑者看不到的范围外权限被原样保留,且不影响已绑该角色的用户。
**复验**:角色管理顶部「操作者」选李四 → 树上超范围节点被隐藏 → 保存只影响其可见范围。

## 六、用户管理 + 切换登录

| 编号 | 场景 | 预期 | 实际 | 状态 |
|---|---|---|---|---|
| F1 | 新建用户绑定"园区A值班员"角色 | 创建成功 | uid 分配成功 | ✅ |
| F1′| 该用户应用菜单数 | 7(与园区A值班员一致) | 7 | ✅ |
| F1″| 该用户点根区域 | 暂无操作权限 | accessible:false | ✅ |

**复验**:用户管理 → 新建用户绑角色 → 右上角"当前登录"切到该用户 → 应用端体验。

## 七、持久化(MySQL)

| 编号 | 场景 | 预期 | 实际 | 状态 |
|---|---|---|---|---|
| G1 | 新建角色后重启服务,角色仍在 | 在,menuIds 不变 | 在,[107] | ✅ |
| G2 | 新建用户后重启服务,用户仍在 | 在 | 在 | ✅ |

**复验**:建角色/用户 → 关闭并重新 `go run main.go` → 数据仍在(从 DB 重载)。

## 八、后台管理域体验(系统管理菜单 / 区域管理 / 组织管理)

> 与应用域区分:应用域=看监控(资源范围 RES_AREA);**管理域=后台配置(系统菜单 + 安保区域管理 AREA + 组织管理 ORG)**。

| 编号 | 场景 | 预期 | 实际 | 状态 |
|---|---|---|---|---|
| H1 | 张三系统管理菜单数(全菜单) | 13 | 13 | ✅ |
| H2 | 张三可管理区域树:根=结构祖先 | accessible=false | false | ✅ |
| H3 | 张三可管理区域树:事件图片测试可管理 | accessible=true | true | ✅ |
| H4 | 张三点事件图片测试→列出本区域资源 | 含"枪机01" | 含枪机01 | ✅ |
| H5 | 张三点根区域→暂无管理权限 | accessible=false | false | ✅ |
| H6 | 李四系统管理菜单数(他只有应用权限) | 0 | 0 | ✅ |
| H7 | 李四可管理区域树(无AREA管理权) | 空 | [] | ✅ |

**说明**:H6/H7 证明"应用域有权 ≠ 管理域有权"——李四能在应用端看监控,但后台管理看不到任何菜单和可管理区域。
**复验**:后台管理(体验)Tab,切到张三看系统菜单+区域/组织管理树;切到李四应为空。

## 九、区域增删改 + 物化路径自动维护(写时鉴权)

> 新增能力:区域真实落库,写操作同样过「功能关(sys.area 菜单)+ 数据关(区域数据权限)」两个维度,
> 并自动维护 `path`——新增=父.path+新ID+"/",移动=整棵子树批量前缀替换,删除=清理对该节点的授权引用。
> 接口:`POST /api/areas?userId=`(新增/重命名/移动)、`POST /api/areas/delete?userId=`。
> 测试日期 2026-06-05,基于干净种子(区域 1~5),测后已复位种子(含 AUTO_INCREMENT)。

| 编号 | 场景 | 预期 | 实际 | 状态 |
|---|---|---|---|---|
| I1 | 李四(无 sys.area 菜单)在区域2下新增 | 功能权限不足 | 拒绝:没有菜单「安保区域管理」 | ✅ |
| I2 | 张三在园区A(id=3,超其 AREA 范围)下新增 | 数据关拒绝 | 拒绝:数据范围未覆盖 | ✅ |
| I3 | 张三在事件图片测试(id=2,范围内)下新增「测试楼A」 | 成功,path=/1/2/6/ | id=6, path=/1/2/6/ | ✅ |
| I4 | 同父下再建同名「测试楼A」 | 同级同名拒绝 | 拒绝 | ✅ |
| I5 | **自动继承**:张三管理新增区域6(只授权了父2子树,未单独配6) | 允许 | 允许(子树包含) | ✅ |
| I6 | **移动**:测试楼B(7,含子8)移到测试楼A(6)下 | 7=/1/2/6/7/,8=/1/2/6/7/8/ | 一致 | ✅ |
| I7 | **防环**:把6移到自己的后代8下 | 拒绝 | 拒绝:不能移到自己/子区域下 | ✅ |
| I8 | 删除非叶子(6,下有7) | 拒绝(有子区域) | 拒绝 | ✅ |
| I9 | 删除叶子(8) | 成功 | 成功 | ✅ |
| I10 | **删除清理授权**:角色显式授权了节点6,删6后该授权 | role_data_scope 中 node 6 行被清理 | areaScopes/resAreaScopes 不再含6 | ✅ |
| I11 | 重启服务后新增/移动结果仍在 | 持久 | 区域7 仍 /1/2/7/ | ✅ |

**说明**:I5/I6 是把「授权子树→新增/移动节点自动继承」真正用起来的关键——移动子树时权限随 `path` 走,无需重配。I10 防止删除后留下悬挂授权。

## 十、超级管理员(is_superuser,仿海康内置 root)

> 新增能力:用户加 `is_superuser` 标志,鉴权三关(菜单/区域/资源)对其**直接放行**,拥有现有及将来全部权限,
> 与数据权限模型解耦(引擎层特例,不依赖任何角色/数据范围)。`tools/dbinit` 幂等补列 + 确保存在一个超管账号。
> 测试日期 2026-06-05,admin 账号 uid=4。

| 编号 | 场景 | 预期 | 实际 | 状态 |
|---|---|---|---|---|
| J1 | admin 访问「高级系统管理」菜单 | 允许 | 允许(超级管理员) | ✅ |
| J2 | admin 访问根区域(张三都被拒,见 B2) | 允许 | 允许 | ✅ |
| J3 | admin 访问园区A(张三无权) | 允许 | 允许 | ✅ |
| J4 | admin 102球机 远程回放(李四被精细排除,见 C2) | 允许 | 允许 | ✅ |
| J5 | admin 系统管理菜单数 | 全 13 | 13 | ✅ |
| J6 | admin 应用菜单数 | 全 11 | 11 | ✅ |
| J7 | admin 可管理区域 accessible | 全 5/5 | 5/5 | ✅ |
| J8 | admin 可管理组织 accessible | 全 4/4 | 4/4 | ✅ |
| J9 | admin 作为委派操作者 grantable | unlimited=true(可授全部) | true | ✅ |
| J10 | dbinit 重复执行 | 不重复建 admin(幂等) | 跳过 | ✅ |

**说明**:J2/J3/J4 是关键——这些正是普通角色被拒的用例(B2/C2 及张三无园区A权),超管全部放行,证明"绕过引擎"语义成立。J9 证明超管委派时 GrantableSet 返回不受限,合并直接采用提交。

## 十一、组织树增删改 + 物化路径自动维护(写时鉴权)

> 新增能力:组织树(组织机构)真实落库,与区域(§九)完全对称。写操作同样过「功能关(人员信息 sys.person.info 菜单)+ 数据关(组织数据权限 ORG)」两个维度,
> 并自动维护 `path`——新增=父.path+新ID+"/",移动=整棵子树批量前缀替换,删除=清理对该节点的 ORG 授权引用。
> **与区域唯一不同**:组织的"非空"判定是「无子组织 **且无下属用户**」(区域是无资源;用户经 `user.org_id` 归属组织)。
> 接口:`POST /api/orgs?userId=`(新增/重命名/移动)、`POST /api/orgs/delete?userId=`。
> 测试日期 2026-06-08,基于干净种子(组织 1~4:根组织>技术部>安防组、行政部),测后已复位种子(含 AUTO_INCREMENT)。
> 操作人:张三(uid=1,有人员信息菜单 + ORG 根子树范围);李四(uid=2,无人员信息菜单,验功能关)。

| 编号 | 场景 | 预期 | 实际 | 状态 |
|---|---|---|---|---|
| O1 | 李四(无 sys.person.info 菜单)在技术部下新增 | 功能权限不足 | 拒绝:没有菜单「人员信息」 | ✅ |
| O3 | 张三在技术部(id=2,范围内)下新增「测试组A」 | 成功,path=/1/2/5/ | id=5, path=/1/2/5/ | ✅ |
| O4 | 同父下再建同名「测试组A」 | 同级同名拒绝 | 拒绝 | ✅ |
| O5 | **自动继承**:张三管理新增组织5(只授权了 ORG 根子树,未单独配5) | 允许 | 允许(子树包含) | ✅ |
| O6 | **移动**:测试组B(6,含子C=7)移到测试组A(5)下 | 6=/1/2/5/6/,7=/1/2/5/6/7/ | 一致(子孙批量重写) | ✅ |
| O7 | **防环**:把5移到自己的后代7下 | 拒绝 | 拒绝:不能移到自己/子组织下 | ✅ |
| O8 | 删除非叶子(6,下有7) | 拒绝(有子组织) | 拒绝 | ✅ |
| O9 | **删有下属用户**:删安防组(3,下有张三) | 拒绝(还有用户) | 拒绝:还有用户(张三) | ✅ |
| O10 | 删除叶子(7,无子无用户) | 成功 | 成功 | ✅ |
| O11 | **删除清理授权**:角色显式授权了组织D,删D后该授权 | role_data_scope 中 node D 的 ORG 行被清理 | orgScopes 不再含D | ✅ |
| O12 | 重启服务后新增/移动结果仍在 | 持久 | 测试组B 仍 /1/2/5/6/ | ✅ |

**说明**:与区域 §九 对照,验证同一套 path-维护引擎在组织树上同样成立。O5/O6 是「授权子树→新增/移动节点自动继承」的关键;O9 是组织相对区域的特有约束(删空组织前须先移走下属用户);O11 防止删除后留下悬挂的 ORG 授权。**12/12 通过**(O2 数据关拒绝复用 §九 I2 已验证的共享 `checkTreeScope` 引擎,种子下张三 ORG 范围覆盖全树故未单列)。

## 十二、角色删除 + 委派校验 + 级联清理

> 新增能力:角色真实删除(`service/role.go` 的 `DeleteRole`)。委派语义与已实测的模型 A 自洽:
> **不受限(actor=0)/超级管理员可删任意;普通操作人须有「角色管理」菜单(sys.person.role,功能关)且只能删自己创建的角色(`created_by`)**。
> 配套修复:`created_by` 改为**仅新建时记操作人、编辑时保持原值**(原先每次保存都覆盖成当前操作人,导致"创建人"不可靠)。
> 删除策略(对齐用户约定):**角色即便仍绑着用户也直接删,不拦**——事务级联清理 `role` + `role_menu` + `role_data_scope` + `role_resource_action` + **`user_role`**;丢了该角色的用户即失去这部分权限,若不再有其他角色则登录后无任何权限。
> 接口:`POST /api/roles/delete?actor=`(body 含 id)。
> 测试日期 2026-06-08,基于干净种子(角色 1~2、用户 1~4),测后已 DROP + dbinit 复位干净种子(含 AUTO_INCREMENT)。

| 编号 | 场景 | 预期 | 实际 | 状态 |
|---|---|---|---|---|
| K1 | 张三(actor=1)新建角色 | createdBy=1 | createdBy=1 | ✅ |
| K2 | **created_by 不被覆盖**:用 actor=0 编辑 K1 角色后 | createdBy 仍=1 | 仍=1 | ✅ |
| K3 | **功能关**:李四(无 sys.person.role 菜单)删该角色 | 拒绝(功能权限不足) | 拒绝:没有菜单「角色管理」 | ✅ |
| K3′| K3 被拒后角色仍在 | 在 | 在 | ✅ |
| K4 | **委派**:张三删园区A值班员(createdBy=0,非自建) | 拒绝(只能删自己创建的) | 拒绝 | ✅ |
| K4′| K4 被拒后园区A值班员仍在 | 在 | 在 | ✅ |
| K5 | 张三删自己创建的角色 | 成功 | 成功,角色消失 | ✅ |
| K6 | **超管绕过**:admin(超管)删非自建角色(createdBy≠4) | 成功 | 成功 | ✅ |
| K7 | **级联清理**:含菜单/区域范围/资源范围/精细操作的角色,删后各关联表 | 无悬挂行 | role_menu/role_data_scope/role_resource_action 均无该 role_id | ✅ |
| K8 | **绑用户直接删**:角色仍绑着用户 U,actor=0 删除 | 成功(不拦) | 成功 | ✅ |
| K8′| 删后用户 U 的 user_role 绑定 | 被清(roleIds 空) | 空 | ✅ |
| K8″| 删后用户 U 的应用菜单 | 0(无任何权限) | 0 | ✅ |
| K9 | 重启服务后:新建角色仍在 | 持久 | 在 | ✅ |
| K9′| 重启服务后:已删角色不再出现 | 持久 | 不在,回到[1,2] | ✅ |

**说明**:K2 是配套修复的关键证明(让"只能删自己创建的"成立);K3/K4 证明委派双重闸门(功能关 + created_by);K6 证明超管绕过;K7/K8′ 在 DB 层面核对了**零悬挂引用**(含 `user_role`);K8″ 证明"丢角色即失权"。**14/14 通过**(自动化 13 项 + DB 级联核对 K7;K9/K9′ 含两次重启)。

## 十三、资源(摄像头)增删改 + 写时鉴权 + 删除清理

> 新增能力:业务资源(摄像头)真实增删改(`service/resource.go` 的 `SaveResource`/`DeleteResource`),补齐**数据树增删改三件套**(区域§九、组织§十一、资源§十三)。
> 资源与区域/组织不同:它是**挂在安保区域下的叶子对象**(`Resource.AreaId`),无树无 path。写操作过两个维度:
> **功能关 = 「资源管理」菜单(sys.resource)**;**数据关 = 对资源所在区域的「安保区域管理」权限(`CheckArea`)**(资源是管理域设备,移动还要对新区域有权)。
> 新增=挂到区域下(授权了该区域 RES_AREA 子树的角色**自动继承**新资源);移动=改 `area_id`(精细授权按资源 id 走,随资源迁移不清);删除=同步清理 `role_resource_action` 对该资源的精细授权行。
> 接口:`POST /api/resources?userId=`(新增/重命名/改类型/移动)、`POST /api/resources/delete?userId=`。
> 测试日期 2026-06-08,操作人:张三(uid=1,有 sys.resource 菜单 + AREA 范围={事件图片测试(2)})、李四(uid=2,无 sys.resource)、admin(uid=4,超管)。测后已 DROP + dbinit 复位干净种子。

| 编号 | 场景 | 预期 | 实际 | 状态 |
|---|---|---|---|---|
| R1 | **功能关**:李四(无 sys.resource 菜单)在区域2新增 | 拒绝(功能权限不足) | 拒绝:没有菜单「资源管理」 | ✅ |
| R2 | **数据关**:张三在范围外区域(园区A=3)新增 | 拒绝(数据范围未覆盖) | 拒绝:无权在「园区A」下新增 | ✅ |
| R3a | 张三在范围内(事件图片测试=2)新增「测试球机A」(dome) | 成功 | 成功 | ✅ |
| R3b | 新资源落库(area=2,type=dome) | 一致 | 一致 | ✅ |
| R3c | **自动继承**:张三(RES_AREA 含区域2)对新资源 live | 允许(继承模式,无精细) | 允许 | ✅ |
| R4 | 同区域再建同名「测试球机A」 | 同区域同名拒绝 | 拒绝 | ✅ |
| R5 | 重命名 +改类型(测试球机A2 / gun,同区域) | 成功 | name/type 已变 | ✅ |
| R6a | **移动防越权**:张三把资源移到范围外区域(园区A) | 拒绝(无权移动) | 拒绝 | ✅ |
| R6b | 超管移动到园区A/1号楼(area=4) | 成功,area_id=4 | 一致 | ✅ |
| R7a | **删除防越权**:张三删范围外资源(102@1号楼) | 拒绝(无权删除) | 拒绝 | ✅ |
| R7b | 超管删资源102 | 成功,资源表消失 | 成功 | ✅ |
| R7c | **删除清理精细授权**:102 删后,角色2 对 102 的 live 精细授权 | role_resource_action 中 102 行被清 | resourceActions 不再含102 | ✅ |
| R8 | 重启服务后:新增资源仍在 / 已删资源不再出现 | 持久 | 建在、删无 | ✅ |

**说明**:R3c 证明「授权区域 RES_AREA 子树 → 新增资源自动继承全部操作」(对应海康"新增资源继承所在区域");R6a/R7a 证明写操作同样过数据关(只能动自己有权区域里的资源);R7c 防止删资源后留下悬挂的精细授权。**13/13 通过**(自动化 12 项 + R8 含两次重启)。

---

## 十四、显式角色范围(模型 B · 受控委派第二维)

> 新增能力:对照真实海康 iSecure Center(见 `docs/海康对照.md`)补齐**显式角色范围**。
> 之前委派只有**模型 A**(只能管自己创建的角色,`created_by`);现在每个角色可显式配置「**可管理哪些其他角色**」(复用 `role_data_scope` 的 `scope_type='ROLE'`,`node_id`=被管理角色 id)。
> **可管理角色集 `manageable(actor)` = (自己创建的角色) ∪ (本人各角色「角色范围」的并集)**(单层不级联,与模型 A 自洽)。该集合统一驱动:角色列表 canEdit/canDelete、编辑/删除门禁、以及「角色范围」维度自身的可授范围。对齐海康原文「可选择的角色范围 = 勾选的角色 ∪ 用户自行创建的角色」。
> 落点:`service/delegation.go`(`Grantable.RoleIds`/`GrantableSet`/`MergeDelegated` 第 5 维度/`ManageableRoles`)、`service/role.go`(`GuardManageRole` 编辑删除共用门禁 + 删除级联清 ROLE 引用)、`perm.go`(saveRole 编辑门禁)、`index.html`(系统管理权限新增「角色范围」子 Tab + 角色列表 🔒/canManage)。
> 测试日期 2026-06-09,**非破坏式**:新建 throwaway 角色 T(`createdBy=0`),经 role1(张三的角色)的「角色范围」授予后由张三管理再删除,全程 seed 不变(role1 临时加的范围在删 T 后自动清理)。操作人:张三(uid=1,持 role1,含 `sys.person.role` 菜单)、admin(uid=4,超管)、不受限(actor=0)。

| 编号 | 场景 | 预期 | 实际 | 状态 |
|---|---|---|---|---|
| MB1 | 不受限(actor=0)新建角色 T(含张三看不到的区域 nodeId=3) | 成功,createdBy=0 | 成功 | ✅ |
| MB2 | `grantable(张三=1).roleIds` 是否含 T | 不含(非自建、role1 无角色范围) | `roleIds=[]` 不含 | ✅ |
| MB3 | 张三(actor=1)删除 T | 拒绝(不在可管理角色范围内) | 拒绝 | ✅ |
| MB4 | 张三(actor=1)编辑 T(改名) | 拒绝(对齐海康 canEdit=false) | 拒绝 | ✅ |
| MB5 | 不受限给 role1 配置「角色范围」=[T](保留 role1 原全部配置) | 成功,role1.roleScopes 含 T | 成功 | ✅ |
| MB6 | `grantable(张三=1).roleIds` 是否含 T | 现含(经由 role1 的角色范围) | `roleIds=[T]` 含 | ✅ |
| MB7a | 张三(actor=1)编辑 T 改名 | 成功(T 已在可管理范围) | 成功 | ✅ |
| MB7b | **合并保留**:张三无权的区域 nodeId=3 | 编辑后仍保留(`preserved≥1`) | `preserved=1`,T 仍含区域3 | ✅ |
| MB8 | 张三(actor=1)删除 T | 成功 | 成功 | ✅ |
| MB9 | **引用清理**:role1 对 T 的「角色范围」引用 | 删 T 后自动清理 | role1.roleScopes 不再含 T | ✅ |
| MB10 | 回归:张三删系统角色 role2(created_by=0、无范围) | 拒绝(向后兼容) | 拒绝 | ✅ |
| MB11 | 回归:admin(超管)`grantable.unlimited` | true(可管理任意角色) | true | ✅ |

**说明**:MB2/MB3/MB4 证明无范围时连「看得到的角色」也改删不得;MB5/MB6 证明显式角色范围把目标角色纳入操作人的可管理集;MB7b 证明角色范围维度同样走「(提交∩可授)∪(原有\可授)」合并、范围外原值不丢;MB9 与 DeleteArea 清授权引用对称,防悬挂引用;MB10/MB11 证明模型 A 行为(只能管自建 / 超管不受限)未回归。**11/11 通过**。

---

## 附:未自动化、建议手工验证的点

1. **新增区域自动继承**:在区域树下(单根)新增子区域并写对 `path`,授权了上层子树的角色应自动覆盖新区域(已单独验证,见对话记录)。
2. **前端交互细节**:树展开/折叠、保存后提示、置灰按钮的点击 toast 等,建议在浏览器实际点一遍。
3. **多角色叠加**:王五(uid=3,双角色)的权限应是两角色的并集,可在应用端登录王五观察。
